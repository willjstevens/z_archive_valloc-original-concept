/*
 * Property of Will Stevens
 * All rights reserved.
 */
package com.valloc.session;


/**
 * 
 * 
 * @author wstevens
 */
abstract class AbstractClientSessionManager implements SessionManager
{
	// SessionId newSessionId(final String username, final int transportId) {
	// SessionId newSessionId(final String username) {
	// SessionId sessionId = null;
	// final UUID uuid = UuidFactory.newInstance();
	// // sessionId = new SessionId(uuid, transportId, username);
	// sessionId = new SessionId(uuid, username);
	// return sessionId;
	// }
}
